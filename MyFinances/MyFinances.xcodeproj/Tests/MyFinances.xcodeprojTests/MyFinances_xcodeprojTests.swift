import XCTest
@testable import MyFinances_xcodeproj

final class MyFinances_xcodeprojTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MyFinances_xcodeproj().text, "Hello, World!")
    }
}
